function solve(num) {
    for (let columns = 0; columns < num; columns++) {
      let resultRow = ''
      
        for (let rows = 1; rows <= num; rows++) {
            
            resultRow += num + " "
          
      }
        console.log(resultRow)
    }
}

solve(3)